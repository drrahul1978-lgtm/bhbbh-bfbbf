{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The smallest set of permissions EVE needs: a window, and the ability to save and open the files holding what she has learned. Nothing that reaches the filesystem unasked, runs a command, or opens a network connection.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-save",
    "dialog:allow-open"
  ]
}
