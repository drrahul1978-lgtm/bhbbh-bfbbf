# Adapters Eve writes for herself at runtime — she regenerates these on demand,
# and they are specific to your own network addresses.
eve-skills/

node_modules/
*.log

# EVE's runtime state: logs, secrets, learned specs. Never committed.
eve-data/
eve.config.json
*.prev

# Desktop app build output — regenerated by tools/build-dist.js and cargo.
dist/
src-tauri/target/
src-tauri/gen/schemas/
